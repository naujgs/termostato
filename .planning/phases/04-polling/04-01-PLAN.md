---
phase: 04-polling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Termostato/Termostato/TemperatureViewModel.swift
autonomous: false
requirements:
  - POLL-01
must_haves:
  truths:
    - "Timer fires every 10 seconds (3 console prints appear within ~30s of app launch)"
    - "Step-chart history retains 360 data points — ring buffer holds a full 60-minute window at 10s cadence"
    - "Chart sub-label reads 'Session history (last 60 min)' — no stale interval numbers in UI"
  artifacts:
    - path: "Termostato/Termostato/TemperatureViewModel.swift"
      provides: "Updated polling interval and ring-buffer capacity"
      contains: "private static let maxHistory = 360"
    - path: "Termostato/Termostato/TemperatureViewModel.swift"
      provides: "Updated timer interval"
      contains: "Timer.publish(every: 10, on: .main, in: .common)"
  key_links:
    - from: "TemperatureViewModel.startPolling()"
      to: "Timer.publish(every:)"
      via: "Combine sink on .main RunLoop"
      pattern: "Timer\\.publish\\(every: 10"
    - from: "TemperatureViewModel.updateThermalState()"
      to: "Self.maxHistory"
      via: "ring-buffer trim — history.count >= Self.maxHistory"
      pattern: "private static let maxHistory = 360"
---

<objective>
Reduce the thermal-state polling interval from 30 seconds to 10 seconds and expand the ring-buffer capacity from 120 to 360 entries so the step-chart retains a full 60-minute history window at the new cadence.

Purpose: POLL-01 — 3x more responsive readings with identical wall-clock history depth.
Output: Two numeric literals changed in TemperatureViewModel.swift, four stale comments updated in the same file. ContentView.swift is confirmed unchanged (label already correct).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polling/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update polling interval and ring-buffer capacity in TemperatureViewModel.swift</name>
  <files>Termostato/Termostato/TemperatureViewModel.swift</files>
  <read_first>
    - Termostato/Termostato/TemperatureViewModel.swift (read in full — executor must see exact line numbers and surrounding context before editing)
  </read_first>
  <action>
Make the following targeted edits to TemperatureViewModel.swift. Do NOT change any other code or logic.

**Edit 1 — Ring-buffer capacity doc-comment and constant (line 48-49):**

Change from:
```swift
/// Session history ring buffer — max 120 readings (D-05). Session-only, never persisted (D-06).
private static let maxHistory = 120
```
To:
```swift
/// Session history ring buffer — max 360 readings (D-05). Session-only, never persisted (D-06).
private static let maxHistory = 360
```

**Edit 2 — Timer interval (line 111):**

Change from:
```swift
timerCancellable = Timer.publish(every: 30, on: .main, in: .common)
```
To:
```swift
timerCancellable = Timer.publish(every: 10, on: .main, in: .common)
```

**Edit 3 — Stale doc-comment on startPolling() (line 101):**

Change from:
```swift
/// Start the 30-second polling timer. Call when scenePhase becomes .active.
```
To:
```swift
/// Start the 10-second polling timer. Call when scenePhase becomes .active.
```

**Edit 4 — Stale inline comment after immediate read (line 116):**

Change from:
```swift
// Immediately read on start so the UI shows data without a 30-second delay.
```
To:
```swift
// Immediately read on start so the UI shows data without a 10-second delay.
```

No other lines may change. The trim logic at line 144 (`if history.count >= Self.maxHistory`) already uses `Self.maxHistory` and requires no modification.
  </action>
  <verify>
    <automated>grep -n "private static let maxHistory = 360" /Users/jgs/workspace/Termostato/Termostato/Termostato/TemperatureViewModel.swift &amp;&amp; grep -n "Timer.publish(every: 10" /Users/jgs/workspace/Termostato/Termostato/Termostato/TemperatureViewModel.swift &amp;&amp; ! grep -q "maxHistory = 120" /Users/jgs/workspace/Termostato/Termostato/Termostato/TemperatureViewModel.swift &amp;&amp; ! grep -q "every: 30" /Users/jgs/workspace/Termostato/Termostato/Termostato/TemperatureViewModel.swift &amp;&amp; echo "PASS: all constants updated, no stale values"</automated>
  </verify>
  <acceptance_criteria>
    - TemperatureViewModel.swift contains exactly `private static let maxHistory = 360`
    - TemperatureViewModel.swift contains exactly `Timer.publish(every: 10, on: .main, in: .common)`
    - TemperatureViewModel.swift does NOT contain `maxHistory = 120`
    - TemperatureViewModel.swift does NOT contain `every: 30`
    - TemperatureViewModel.swift does NOT contain `120 readings`
    - TemperatureViewModel.swift does NOT contain `30-second`
    - All other logic (trim condition at line 144, background path, notification gate, stopPolling) is unchanged
  </acceptance_criteria>
  <done>Four targeted edits applied; all six grep assertions pass; no unintended changes to surrounding code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify 10-second polling cadence in Simulator</name>
  <files>none</files>
  <read_first>
    - (No file edits — Simulator verification only)
  </read_first>
  <action>Build and run in Simulator. Observe console output for 30 seconds to confirm 10s polling cadence. Confirm UI label accuracy. No code changes are made in this task.</action>
  <what-built>Two numeric literal changes and four comment updates in TemperatureViewModel.swift that reduce polling from 30s to 10s and expand ring-buffer capacity from 120 to 360.</what-built>
  <how-to-verify>
1. Open Xcode and build the Termostato scheme for the Simulator (any iPhone simulator, iOS 18+).
2. Launch the app in the Simulator.
3. Watch the Xcode debug console for `[Termostato] thermalState = ...` log lines.
4. Confirm 3 log lines appear within approximately 30 seconds (10s cadence, not 30s cadence).
5. Confirm the chart sub-label in the app reads "Session history (last 60 min)" — no stale text.
  </how-to-verify>
  <verify>
    <automated>MISSING — 10s cadence requires runtime Simulator observation; no XCTest target exists in this project. Manual verification per how-to-verify steps above substitutes.</automated>
  </verify>
  <acceptance_criteria>
    - 3 console log lines (`[Termostato] thermalState = ...`) appear within ~30 seconds of app launch
    - Chart sub-label reads exactly "Session history (last 60 min)"
    - No build errors or Swift compilation warnings introduced by the edits
  </acceptance_criteria>
  <done>10s cadence confirmed in Simulator console; chart label verified; build clean.</done>
  <resume-signal>Type "approved" when the 10s cadence is confirmed, or describe any issue observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This phase changes two numeric literals and updates four inline comments. No data crosses any trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | N/A | TemperatureViewModel.swift | accept | Phase is two numeric literal changes (30 to 10, 120 to 360) and four comment updates. No authentication, session management, input validation, network activity, or cryptography is involved. Security domain is not applicable to this phase. |
</threat_model>

<verification>
After task 1 completes:
- `grep -n "private static let maxHistory = 360" Termostato/Termostato/TemperatureViewModel.swift` returns a match
- `grep -n "Timer.publish(every: 10" Termostato/Termostato/TemperatureViewModel.swift` returns a match
- `grep "every: 30" Termostato/Termostato/TemperatureViewModel.swift` returns empty
- `grep "maxHistory = 120" Termostato/Termostato/TemperatureViewModel.swift` returns empty

After task 2 (Simulator checkpoint):
- Debug console shows `[Termostato] thermalState = ...` at ~10s intervals
- Chart sub-label reads "Session history (last 60 min)"
- No build errors
</verification>

<success_criteria>
1. Thermal state readings update every 10 seconds (3 console prints within ~30s of launch)
2. Ring buffer holds 360 data points — history window remains 60 minutes at 10s cadence (10 x 360 = 3,600s)
3. Chart sub-label "Session history (last 60 min)" is accurate and unchanged
4. No regressions — background thermal observer, notification gate, and cooldown logic are unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/04-polling/04-01-SUMMARY.md` using the standard summary template.
</output>
