---
phase: 01-foundation-device-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - .planning/phases/01-foundation-device-validation/DECISION-IOKIT.md
autonomous: false
requirements:
  - INST-01

must_haves:
  truths:
    - "The app installs on the physical device via Xcode sideload under a free Apple ID"
    - "Console output shows at least one '[CoreWatch] thermalState = ...' line confirming ProcessInfo poll works"
    - "Console output shows one '[CoreWatch][IOKit]' line stating whether access is granted or blocked"
    - "A written decision record exists at .planning/phases/01-foundation-device-validation/DECISION-IOKIT.md with the exact console line pasted in"
    - "The IOKit probe code and its init() call are deleted from TemperatureViewModel.swift"
  artifacts:
    - path: ".planning/phases/01-foundation-device-validation/DECISION-IOKIT.md"
      provides: "Written decision record — Phase 1 success criterion #3"
      contains: "IOKit"
  key_links:
    - from: "Xcode console output"
      to: "DECISION-IOKIT.md"
      via: "Human copies exact log line into decision record"
      pattern: "\\[CoreWatch\\]\\[IOKit\\]"
---

<objective>
Install the app on the physical target device, capture the IOKit probe console output, write the decision record, then delete the IOKit probe code to leave Phase 2 with a clean ViewModel.

Purpose: Phase 1 success criterion #3 requires a written decision record stating whether IOKit returns data or is silently blocked. This plan produces that record. Criterion #4 (backgrounding pauses the timer) is also confirmed here.

Output: DECISION-IOKIT.md with observed result. TemperatureViewModel.swift with probe code removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jgs/workspace/CoreWatch/.planning/phases/01-foundation-device-validation/01-CONTEXT.md
@/Users/jgs/workspace/CoreWatch/.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Install on device and capture console output</name>
  <read_first>
    - /Users/jgs/workspace/CoreWatch/.planning/phases/01-foundation-device-validation/01-CONTEXT.md (success criteria: 4 items)
    - /Users/jgs/workspace/CoreWatch/CLAUDE.md (Code Signing / Sideloading section: free Apple ID install steps)
  </read_first>
  <what-built>
    TemperatureViewModel with ProcessInfo.thermalState polling (30s) and one-shot IOKit probe.
    ContentView with scenePhase wiring (start/stop polling on foreground/background).
    Xcode project targeting iOS 18.0, signed with personal team.
  </what-built>
  <how-to-verify>
    1. Connect your iPhone via USB.
    2. In Xcode, select your device in the run destination selector (not a simulator).
    3. Product → Run (Cmd+R) with your device selected.
    4. If prompted "verify developer app" on device: Settings → General → VPN & Device Management → trust your developer certificate.
    5. Once the app launches, open Xcode → View → Debug Area → Activate Console (Cmd+Shift+C).
    6. Wait up to 5 seconds and look for this line pattern:
         [CoreWatch][IOKit] ...
       Copy this EXACT line. It will say one of:
         - "[CoreWatch][IOKit] Temperature key found: ..." — IOKit ACCESS GRANTED
         - "[CoreWatch][IOKit] IORegistryEntryCreateCFProperties failed ... BLOCKED by AMFI/sandbox"
         - "[CoreWatch][IOKit] IOServiceGetMatchingService returned 0 — service not found (BLOCKED...)"
    7. Also verify you see at least one line matching:
         [CoreWatch] thermalState = nominal
    8. Background the app (press Home or swipe up) and verify:
         [CoreWatch] Polling stopped (backgrounded).
    9. Foreground the app and verify:
         [CoreWatch] Polling started.

    SUCCESS looks like:
    ```
    [CoreWatch][IOKit] IORegistryEntryCreateCFProperties failed (kr=...) — BLOCKED by AMFI/sandbox
    [CoreWatch] Polling started.
    [CoreWatch] thermalState = nominal
    ```
    (The exact IOKit line content is the key data — paste it regardless of outcome.)
  </how-to-verify>
  <acceptance_criteria>
    - App launched on physical device without "Untrusted Developer" error blocking it
    - Console shows exactly one "[CoreWatch][IOKit]" line
    - Console shows at least one "[CoreWatch] thermalState =" line
    - Console shows "[CoreWatch] Polling stopped" when app backgrounded
    - Console shows "[CoreWatch] Polling started" when app foregrounded again
  </acceptance_criteria>
  <resume-signal>
    Paste the exact "[CoreWatch][IOKit] ..." console line here, then type "verified" to continue.
    Example: "[CoreWatch][IOKit] IORegistryEntryCreateCFProperties failed (kr=1) — BLOCKED by AMFI/sandbox" → verified
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write decision record and remove IOKit probe</name>
  <read_first>
    - CoreWatch/CoreWatch/TemperatureViewModel.swift (to locate probeIOKit() method and its call in init() before deletion)
    - /Users/jgs/workspace/CoreWatch/.planning/phases/01-foundation-device-validation/01-CONTEXT.md (D-02: probe removal is unconditional — no #if DEBUG gate)
  </read_first>
  <files>
    .planning/phases/01-foundation-device-validation/DECISION-IOKIT.md
    CoreWatch/CoreWatch/TemperatureViewModel.swift
  </files>
  <action>
Step 1 — Write DECISION-IOKIT.md using the exact console line captured in Task 1.

File content template (fill in the bracketed values from Task 1 output):

```markdown
# IOKit Probe Decision Record

**Date observed:** {today's date}
**Device:** {device model, e.g. "iPhone 14 Pro, iOS 18.x"}
**Signing:** Free Apple ID personal team (7-day certificate)
**Xcode version:** 26.4.1

## Observed Console Output

```
{PASTE THE EXACT [CoreWatch][IOKit] LINE HERE}
```

## Decision

{One of the following — choose based on observed output:}

**BLOCKED:** IOKit `IOPMPowerSource` Temperature key is not accessible under free Apple ID sideloading.
AMFI enforces the `systemgroup.com.apple.powerlog` entitlement requirement at runtime.
The app will display thermal state via `ProcessInfo.thermalState` only.
Numeric °C display is confirmed Out of Scope for v1.

OR

**ACCESSIBLE:** IOKit `IOPMPowerSource` Temperature key returned a value.
This is unexpected under standard sideloading. Document the exact conditions
(device, iOS version, signing) and revisit REQUIREMENTS.md Out of Scope entry.

## Impact on Architecture

- `TemperatureViewModel` continues with `ProcessInfo.thermalState` as the sole data source.
- No fallback "—°C" UI needed; the app displays thermal state levels only.
- Phase 2 and Phase 3 proceed as planned.
```

Step 2 — Remove the IOKit probe from TemperatureViewModel.swift (D-02: unconditional deletion, no #if DEBUG):

Delete from TemperatureViewModel.swift:
1. The entire `probeIOKit()` method (from `// MARK: - IOKit Probe` comment through the closing `}`)
2. The `probeIOKit()` call in `init()`
3. The `// D-01: One-shot IOKit probe...` comment in `init()`

After deletion, `init()` should be empty (or contain only the default empty body `{}`).

Step 3 — Rebuild to confirm probe removal compiles cleanly:
```
xcodebuild -project CoreWatch/CoreWatch.xcodeproj \
  -scheme CoreWatch \
  -destination "generic/platform=iOS" \
  -configuration Debug \
  CODE_SIGNING_ALLOWED=NO \
  build
```

The bridging header and IOKit.framework linkage remain — they are not harmful to keep and may be referenced
by internal documentation. Only the Swift probe code is removed.
  </action>
  <verify>
    <automated>
      grep -c "probeIOKit" /Users/jgs/workspace/CoreWatch/CoreWatch/CoreWatch/TemperatureViewModel.swift; \
      xcodebuild -project /Users/jgs/workspace/CoreWatch/CoreWatch/CoreWatch.xcodeproj \
        -scheme CoreWatch \
        -destination "generic/platform=iOS" \
        -configuration Debug \
        CODE_SIGNING_ALLOWED=NO \
        build 2>&1 | tail -3
    </automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/01-foundation-device-validation/DECISION-IOKIT.md exists
    - grep "IOKit" .planning/phases/01-foundation-device-validation/DECISION-IOKIT.md returns a match
    - grep "CoreWatch][IOKit]" .planning/phases/01-foundation-device-validation/DECISION-IOKIT.md returns at least one line (the pasted console line)
    - grep "probeIOKit" CoreWatch/CoreWatch/TemperatureViewModel.swift returns 0 matches (probe fully deleted)
    - grep "IOKit Probe" CoreWatch/CoreWatch/TemperatureViewModel.swift returns 0 matches (MARK comment deleted)
    - xcodebuild exits 0 ("BUILD SUCCEEDED") after probe deletion
    - The decision record contains one of the strings "BLOCKED" or "ACCESSIBLE"
  </acceptance_criteria>
  <done>
    DECISION-IOKIT.md exists with the observed console line and a clear BLOCKED/ACCESSIBLE verdict.
    TemperatureViewModel.swift has zero references to probeIOKit.
    Project builds clean — Phase 2 can start immediately.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer Mac → physical device (USB) | Xcode code signing gate; free Apple ID limits what can run |
| Decision record file → planning system | Plaintext file; no secrets, no PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | DECISION-IOKIT.md | accept | Contains only device model and iOS version — developer's own device, stored in private local repo. No secrets, no credentials, no PII. |
| T-03-02 | Tampering | TemperatureViewModel.swift probe deletion | accept | Deletion is irreversible without git history. The decision record (DECISION-IOKIT.md) preserves the probe's output permanently — the code itself is intentionally discarded. |
| T-03-03 | Denial of Service | Free Apple ID 7-day cert expiry | accept | Known constraint. If cert expires before on-device validation, reinstall via Xcode (Product → Run with device connected). No mitigation needed beyond re-install. |
</threat_model>

<verification>
Phase 1 is complete when ALL four success criteria from ROADMAP.md are TRUE:

1. Build + install: `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` exits 0, and app runs on physical device
2. Polling confirmed: Console shows "[CoreWatch] thermalState = ..." lines
3. Decision record: `cat .planning/phases/01-foundation-device-validation/DECISION-IOKIT.md` exists and contains a BLOCKED or ACCESSIBLE verdict with pasted console evidence
4. scenePhase confirmed: Console shows Polling stopped/started on background/foreground transitions

Final check: `grep -c "probeIOKit" CoreWatch/CoreWatch/TemperatureViewModel.swift` returns 0
</verification>

<success_criteria>
- App installs and runs on physical iPhone under free Apple ID (INST-01)
- ProcessInfo.thermalState polling confirmed working (30s interval, logs to console)
- IOKit result documented in DECISION-IOKIT.md (blocked/accessible + exact console evidence)
- scenePhase timer lifecycle confirmed (start on foreground, stop on background)
- TemperatureViewModel is clean and ready for Phase 2 extension (no probe code)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-device-validation/01-03-SUMMARY.md`
</output>
