---
phase: 06-mach-api-proof-of-concept
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .planning/phases/06-mach-api-proof-of-concept/06-VERDICTS.md
autonomous: false
requirements:
  - CPU-02
  - MEM-02

must_haves:
  truths:
    - "Each of the 4 Mach APIs has a documented verdict: accessible, degraded, or blocked"
    - "Raw evidence (kern_return_t codes, sample values, timestamps) accompanies each verdict"
    - "If system-wide APIs are blocked, the graceful-fallback decision is confirmed"
    - "Phase 7 planner can read 06-VERDICTS.md and know exactly which APIs to integrate"
  artifacts:
    - path: ".planning/phases/06-mach-api-proof-of-concept/06-VERDICTS.md"
      provides: "Per-API verdict report with raw evidence for Phase 7 consumption"
      contains: "## Verdicts"
  key_links:
    - from: ".planning/phases/06-mach-api-proof-of-concept/06-VERDICTS.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 7 reads verdicts to decide which APIs to wire into ViewModel"
      pattern: "accessible|blocked|degraded"
---

<objective>
Run the Mach API probe on a physical iOS 18 device, observe the results in the debug sheet, and document per-API verdicts in 06-VERDICTS.md so Phase 7 knows which APIs to integrate.

Purpose: This is the validation checkpoint — the entire point of Phase 6. Without physical device results, the verdicts are unknown and Phase 7 cannot proceed.
Output: 06-VERDICTS.md with per-API verdicts and raw evidence
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mach-api-proof-of-concept/06-CONTEXT.md
@.planning/phases/06-mach-api-proof-of-concept/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From CoreWatch/CoreWatch/SystemMetrics.swift:
```swift
enum APIVerdict: String {
    case accessible = "Accessible"
    case degraded   = "Degraded"
    case blocked    = "Blocked"
    case pending    = "Pending"
}

@Observable @MainActor
final class SystemMetricsProbe {
    static let cpuAPI = "host_statistics (CPU)"
    static let memoryAPI = "host_statistics64 (Memory)"
    static let processMemoryAPI = "task_info (Process Memory)"
    static let processCPUAPI = "task_threads (Process CPU)"

    private(set) var finalVerdicts: [String: APIVerdict]
    private(set) var results: [String: [MachProbeResult]]
    func runProbeSequence()
}
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run probe on physical iOS 18 device and verify results</name>
  <read_first>
    - CoreWatch/CoreWatch/SystemMetrics.swift (understand what the probe does and what console output to expect)
    - .planning/phases/06-mach-api-proof-of-concept/06-UI-SPEC.md (understand what the debug sheet looks like)
  </read_first>
  <what-built>Mach API probe engine with debug sheet UI (Plan 06-01). The app now has a hidden debug sheet accessible via long-pressing the "CoreWatch" title. The sheet probes 4 Mach kernel APIs and classifies each as Accessible, Degraded, or Blocked.</what-built>
  <how-to-verify>
    1. Connect physical iPhone (iOS 18) to Mac via USB
    2. Open CoreWatch.xcodeproj in Xcode 26.4.1
    3. Select your physical device as build target
    4. Build and run (Cmd+R)
    5. Once the app launches on device, long-press the "CoreWatch" title text for ~0.5 seconds
    6. The debug sheet should appear with "Mach API Probe" title and 4 verdict rows all showing "Pending"
    7. Tap "Run Probe" button
    8. Wait ~30 seconds for 3 samples to complete (watch progress bar: "Sample 1 of 3" -> "Sample 2 of 3" -> "Sample 3 of 3")
    9. After completion, note the verdict badge color and text for each of the 4 APIs:
       - host_statistics (CPU): _____ (green/yellow/red)
       - host_statistics64 (Memory): _____ (green/yellow/red)
       - task_info (Process Memory): _____ (green/yellow/red)
       - task_threads (Process CPU): _____ (green/yellow/red)
    10. Also note the raw data values shown under each API row
    11. Check Xcode console (Cmd+Shift+C) for [CoreWatch] log lines with kern_return_t values
    12. Report back with: the verdict for each API, the kern_return_t values, and any raw data shown
  </how-to-verify>
  <resume-signal>Provide the 4 verdicts and raw evidence. Example: "host_statistics CPU: Accessible, kern_return_t=0, user=12345 system=6789 idle=98765 nice=0. host_statistics64 Memory: Blocked, kern_return_t=5. task_info: Accessible, resident_size=45MB. task_threads: Accessible, 4 threads, 2.3% CPU." Or describe any issues encountered.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 06-VERDICTS.md from device probe results</name>
  <files>.planning/phases/06-mach-api-proof-of-concept/06-VERDICTS.md</files>
  <read_first>
    - .planning/phases/06-mach-api-proof-of-concept/06-CONTEXT.md (D-08, D-09 — verdict document structure and evidence requirements)
    - .planning/REQUIREMENTS.md (CPU-02 and MEM-02 definitions, graceful fallback language)
  </read_first>
  <action>
Create `.planning/phases/06-mach-api-proof-of-concept/06-VERDICTS.md` using the actual device probe results provided by the user in the checkpoint. The document structure must be:

```markdown
# Phase 6: Mach API Verdicts

**Probed:** {date}
**Device:** iPhone (iOS 18.x) — free Apple ID sideload
**Probe method:** 3 samples at 10-second intervals, majority verdict

## Summary

| API | Verdict | kern_return_t | Notes |
|-----|---------|---------------|-------|
| host_statistics (CPU) | {verdict} | {code} | {brief} |
| host_statistics64 (Memory) | {verdict} | {code} | {brief} |
| task_info (Process Memory) | {verdict} | {code} | {brief} |
| task_threads (Process CPU) | {verdict} | {code} | {brief} |

## Detailed Evidence

### host_statistics (CPU) — {VERDICT}

**Requirement:** CPU-02
**kern_return_t:** {code} ({KERN_SUCCESS or error name})
**Samples:**
| Sample | Timestamp | kern_return_t | Data |
|--------|-----------|---------------|------|
| 1 | {time} | {code} | {raw values} |
| 2 | {time} | {code} | {raw values} |
| 3 | {time} | {code} | {raw values} |

**Verdict rationale:** {why this classification}

### host_statistics64 (Memory) — {VERDICT}
{same structure}

### task_info (Process Memory) — {VERDICT}
{same structure}

### task_threads (Process CPU) — {VERDICT}
{same structure}

## Phase 7 Implications

### System-Wide APIs
- CPU (host_statistics): {go/no-go for Phase 7}
- Memory (host_statistics64): {go/no-go for Phase 7}

### Per-Process APIs
- Memory (task_info): {go/no-go for Phase 7}
- CPU (task_threads): {go/no-go for Phase 7}

### Graceful Fallback Decision
{If any system-wide API is blocked: confirm the fallback — hide system-wide gauge, show per-process only. Reference CPU-02 and MEM-02 graceful fallback language from REQUIREMENTS.md.}
{If all APIs accessible: note that no fallback is needed.}
```

Fill ALL fields with actual data from the user's checkpoint report. Do NOT use placeholder values. If the user reported partial data, note what is missing and why.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-mach-api-proof-of-concept/06-VERDICTS.md && grep -c "Verdict" .planning/phases/06-mach-api-proof-of-concept/06-VERDICTS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/06-mach-api-proof-of-concept/06-VERDICTS.md` exists
    - File contains `## Summary` with a 4-row verdict table
    - File contains `## Detailed Evidence` with subsections for all 4 APIs
    - Each API subsection contains a 3-row sample table with timestamps and raw data
    - File contains `## Phase 7 Implications` with go/no-go per API
    - File contains `### Graceful Fallback Decision` section
    - File contains actual kern_return_t values (not placeholders)
    - File references CPU-02 and MEM-02 requirement IDs
  </acceptance_criteria>
  <done>06-VERDICTS.md documents all 4 API verdicts with raw evidence from physical device testing. Phase 7 planner can read this file and make go/no-go decisions per API without re-running the probe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates a documentation file only — no code changes, no runtime impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | 06-VERDICTS.md | accept | Contains system metrics data (CPU ticks, memory page counts) — non-sensitive. File is in .planning/ directory, not shipped in app bundle. |
</threat_model>

<verification>
1. 06-VERDICTS.md exists with complete per-API verdicts
2. Each verdict has raw kern_return_t evidence and sample data
3. Phase 7 implications section clearly states go/no-go per API
4. Graceful fallback decision is documented if any system API is blocked
</verification>

<success_criteria>
- 06-VERDICTS.md contains verdicts for all 4 Mach APIs based on actual device testing
- Each verdict is classified as accessible, degraded, or blocked with raw evidence
- Phase 7 planner can determine which APIs to wire up by reading this document alone
- If system-wide APIs are blocked, the graceful-fallback path is confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/06-mach-api-proof-of-concept/06-02-SUMMARY.md`
</output>
