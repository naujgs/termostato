---
phase: 08-dashboard-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Termostato/Termostato/ContentView.swift
autonomous: true
requirements:
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "Tab selection is tracked by an explicit @State variable, not implicit SwiftUI state"
    - "Switching away from a tab and back does not reset metric values or trigger a re-render flash"
  artifacts:
    - path: "Termostato/Termostato/ContentView.swift"
      provides: "TabView container with explicit selectedTab binding"
      contains: "@State private var selectedTab: Int = 0"
  key_links:
    - from: "ContentView.body"
      to: "TabView"
      via: "TabView(selection: $selectedTab)"
      pattern: "TabView\\(selection: \\$selectedTab\\)"
---

<objective>
Add explicit tab-selection state to ContentView so that SC5 (tab selection persists within a session) can be verified on device.

Purpose: Phase 7 delivered the TabView scaffold with implicit selection state. D-03 requires making selection explicit via `@State private var selectedTab: Int = 0` + `TabView(selection: $selectedTab)`. This is the only Swift code change in Phase 8.
Output: ContentView.swift with `selectedTab` @State property and bound TabView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-dashboard-tabs/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selectedTab @State binding to ContentView</name>
  <files>Termostato/Termostato/ContentView.swift</files>
  <read_first>
    - Termostato/Termostato/ContentView.swift — read the full file before touching it; confirm the current TabView call has no `selection:` parameter
  </read_first>
  <action>
    In `ContentView`, add one property declaration and update the TabView initializer:

    1. After the existing `@State private var metrics = MetricsViewModel()` line (line 8), add:
       ```swift
       @State private var selectedTab: Int = 0
       ```

    2. Change the TabView initializer from:
       ```swift
       TabView {
       ```
       to:
       ```swift
       TabView(selection: $selectedTab) {
       ```

    3. Add `.tag()` modifiers to each tab item so SwiftUI maps the selection integer to the correct tab:
       - `ThermalView(viewModel: vm)` tab → `.tag(0)` (add after `.tabItem { ... }`)
       - `CPUView(metrics: metrics)` tab → `.tag(1)`
       - `MemoryView(metrics: metrics)` tab → `.tag(2)`

    No other changes. Do not modify lifecycle code, onChange, onAppear, or any other part of ContentView.

    The tab integer assignments follow the standard order already established in Phase 7:
    0 = Thermal, 1 = CPU, 2 = Memory.

    Per D-03 in 08-CONTEXT.md.
  </action>
  <verify>
    File contains all three of these exact strings (grep-checkable):
    - `@State private var selectedTab: Int = 0`
    - `TabView(selection: $selectedTab)`
    - `.tag(0)` and `.tag(1)` and `.tag(2)`
  </verify>
  <acceptance_criteria>
    - `grep -n "selectedTab" Termostato/Termostato/ContentView.swift` returns at least 2 matches (declaration + usage in TabView init)
    - `grep -n "TabView(selection: \$selectedTab)" Termostato/Termostato/ContentView.swift` returns 1 match
    - `grep -c "\.tag(" Termostato/Termostato/ContentView.swift` returns 3
    - File compiles without errors (Xcode build or `xcodebuild -scheme Termostato` succeeds — if build toolchain available; otherwise static grep checks are sufficient)
  </acceptance_criteria>
  <done>ContentView.swift has explicit selectedTab @State binding. TabView uses selection: $selectedTab. All three tabs have .tag() modifiers matching 0/1/2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This change is purely local Swift state — no input crosses a trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | ContentView.swift | accept | Single-file local UI state change; no external input, no data persistence, no network. Minimal attack surface. |
</threat_model>

<verification>
grep -n "selectedTab" Termostato/Termostato/ContentView.swift
grep -n 'TabView(selection: \$selectedTab)' Termostato/Termostato/ContentView.swift
grep -c '\.tag(' Termostato/Termostato/ContentView.swift
</verification>

<success_criteria>
ContentView.swift contains `@State private var selectedTab: Int = 0`, `TabView(selection: $selectedTab)`, and three `.tag(N)` modifiers (0, 1, 2). File is syntactically valid Swift.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-tabs/08-01-SUMMARY.md`
</output>
